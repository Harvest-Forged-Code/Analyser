"""Presentation layer: controllers and views (GUI/CLI/web).

Purpose:
    Orchestrate user-facing workflows and render outputs.

Goal:
    Depend on domain interfaces/services only; keep IO confined to views.
"""
