"""Infrastructure layer: persistence, filesystem, external API adapters.

Purpose:
    Contain implementations that perform IO (files, JSON/INI parsing, etc.).

Goal:
    Keep domain and presentation layers free of concrete IO details.
"""
